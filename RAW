#!/bin/bash

# MySQL credentials
USER="your_username"
PASSWORD="your_password"
DATABASE="your_database"

# MySQL command to truncate tables
mysql -u $USER -p$PASSWORD $DATABASE <<EOF
TRUNCATE TABLE sessions;
TRUNCATE TABLE ban_ip;
TRUNCATE TABLE flood;
EOF
